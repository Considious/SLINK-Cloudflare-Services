{
  "name": "slink-mugging-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.js",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
